// import { prisma } from "./db/prisma";

export default function Home() {
  // const users = await prisma.account.findMany();
  // console.log({ users });

  return (
    <h1>Zeptatronic</h1>
  );
}
